# Known Issues

Read when… the plugin misbehaves during play, or before picking a bug to fix. These are **open** bugs with confirmed root causes — resolved traps live in `gotchas.md`. Each entry: Symptom → Root cause → Proposed fix → Notes/risks. When an issue is fixed, move it to `gotchas.md` with the fix and prevention rule.

*No open issues.*

Recently resolved (see `gotchas.md`):

| Was | Now |
|---|---|
| Sequential notes always cut off the previous note's tail | gotchas trap #5 — fixed 2026-07-05 |
| Decay knob: bottom 80% of travel musically useless | gotchas trap #6 — fixed 2026-07-05 |
